#!/usr/bin/env node

'use strict'

const app = require('../app')
const config = require('../config/config')

app.listen(config.port,() => {
  console.info(`app started at the port ${config.port}`)
})
